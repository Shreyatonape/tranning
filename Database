Create table category (category_id SERIAL primary key,name varchar(20),last_update timestamp default current_timestamp);
Create table film_category(film_id SERIAL primary key,category_id int references film_category(category_id),last_update timestamp default current_timestamp);
